import { AbButtonStyles } from './types';

const styles: { [name: string]: AbButtonStyles } = {
  voice: {
    color: 'secondary',
    boxShadow: 3,
  },
  alert: {
    color: 'warning',
    boxShadow: 0,
  },
};

export default styles;
